# CLAUDE.md

本文件为 Claude Code 在本仓库工作的指引。用中文回复；默认自主推进、少问 Yes/No；回答要直接说清"到底做没做"，别只做分析。

## 项目概述

`codex-auto-sms-receiver` = Chrome MV3 扩展 + Python/Flask 后端，自动化完成 Codex/ChatGPT 的 OAuth 账号创建/登录，全程自动接收邮箱 OTP 与短信 OTP。

- **`chrome_plus_ver/`** — MV3 扩展。`sidepanel.js`(侧边栏 UI + 桥接轮询) ↔ `background.js`(service worker，真正操作页面) ↔ Python 后端(HTTP 轮询桥接)。
- **`src/`** — Flask 后端与流水线。
  - `webapp.py` / `app.py` — Web 服务入口。
  - `codex_worker.py` — 单账号流水线子进程入口(`worker_main`)。**每次 job/重试通常是独立进程**。
  - `upstream_bridge.py` — 核心编排：驱动 OAuth 各阶段、等邮箱/短信 OTP、收尾拿 callback。
  - `browser_bridge.py` — 与扩展之间的请求/响应队列。桥接 kind：`navigate` / `reload`(等价按 F5) / `page_action` / `cleanup`(清浏览器) / `page_fetch`(活动页带 cookie 请求，用于读 session) / `proxy_apply`(切换浏览器代理)。
  - `mailbox_store.py` / `hero_sms.py` / `sms_config.py` — 邮箱池、短信 provider(Hero SMS / smsbower)。
- **产物落盘（均带日期子目录）**：OAuth 凭证 → `data/codex_accounts/{YYYY-MM-DD}/codex-{email}.json`（`codex_oauth.save_codex_credential`）；导出的 Session → `data/codex_sessions/{YYYY-MM-DD}/session-{email}.json`（`upstream_bridge._capture_and_save_session`）。`artifact_store` 的凭证读取三处都是 `recursive=True`，能扫到日期子目录；`_artifact_id` 基于相对路径哈希，加子目录不影响顶层旧文件的 id。
- **导出 Session 模式（仅登录，不接码/不 OAuth）**：插件"批量处理账号"处的「导出 Session」按钮 → `POST /api/codex-pipeline` 带 `mode:'session'` → `start_batch(mode='session')` 给每个 mailbox 打 `export_session=True` → worker `run_codex_only` 读该 flag 后走**独立的 `_run_session_export`**（不是完整 OAuth 流程）：只做 `submit_email` + `submit_email_otp`（或密码+TOTP）把账号登录进 chatgpt.com，然后 `page_fetch` `/api/auth/session` 直接拿到 token 落盘。**不走 Codex OAuth authorize、不做手机验证、不点 consent、不消耗短信。** 筛选条件和批量处理一致。踩坑：最初错误地在完整 OAuth 流程末尾加 `capture_session`，导致导 session 也要接码——session 导出只需登录态即可读 session 接口，已改为独立最小路径。
- **登录素材类型 `smsbower_gmail`（接口取号注册，需求见 `docs/smsbower-gmail.md`）**：这是本仓库**唯一会新建账号**的路径，和"不会创建或注册新账号"的原定位相反——导入区那句提示已按素材类型动态切换（`#importLocalNote`）。**三段都已完成**：① 配置 + 取号 + 清单 + 导出；② 注册主流程（步骤 1-6）；③ MFA（步骤 7-10）。
  - **完整链路**：取号入库 → `/create-account` 建号 → 设密码 → 轮询 smsbower 取码 → `/about-you` → 登录态确认 → `#pricing` 探 Plus 资格 → `#settings/Security` 开验证器 App 取 Base32 密钥 → 记录改写成 `password_totp`，素材 = `email----密码----密钥----0/1`。
  - **注册入口是 chatgpt.com 首页，不是 `auth.openai.com/create-account`（直连也一样，2026-08-12 起）**（`_signup_entry_urls(mailbox)`：代理 = `_PROXY_SIGNUP_ENTRY_URLS` 只有首页；直连 = `_SIGNUP_ENTRY_URLS` = 首页 + `/create-account` 后备）。**直连原本保留 `/create-account` 打头，2026-08-12 实测已经不行了**：`logs/codex-7c4f2e28fe801a14090542d3-5bf08f2d-a1.log`（无代理）里 GET `/create-account` **连续 5 轮全是 `Your session has ended`**（`is_missing_session:true`，整页只有一个 Log in 链接、没有任何输入框），每轮都先经 chatgpt.com 重新种会话**一次都没生效**；第二入口 `chatgpt.com/auth/login_with?screen_hint=signup` 被重定向回同一张中转页，再废 5 轮 → 整单失败还白烧一个取号。同一批日志里代理跑的 `codex-0c253ac577af3bc3447052b9` 从首页进弹窗一路注册成功。所以「预热能把 /create-account 的会话种起来」这个前提已经不成立，`?screen_hint=signup` 入口直接删掉，并加了 `_SIGNUP_RESEED_ROUNDS`(2)：非首页入口连续 2 轮还停在中转页就换入口，别再 6 轮 × 2 入口白等 90s。**症状识别：日志刷「注册入口仍是会话中转页，经 chatgpt.com 重新种会话后再进 …」= 这个入口已经死了，不是网络慢。**代理侧更早的依据 `logs/codex-a87e3d33c1101746f29f9537-6782ed72-a1.log`（jp 住宅代理）：`/create-account` 连续 5 轮全部渲染成 `Your session has ended`（每轮都先经 chatgpt.com 重新种会话，无效）；第 6 轮好不容易过去，提交邮箱直接落 `login-password`，点「Sign up」回来再提交还是 `login-password`，第三次 `unknown` 收工——白烧一个取号。新路径：`https://chatgpt.com/` → 点右上角「Log in」→ **「Log in or sign up」二合一弹窗** →「Continue with email」→ 输邮箱 → 后续照旧。**这不违反下面「注册入口和登录入口混用必死」那两条**：被禁的是 `auth.openai.com/log-in` 那个纯登录页、以及"会话已结束"中转页上那条把注册拖进登录的 `Log in` 链接；chatgpt.com 首页的 Log in 按钮进的是二合一弹窗，新地址会走到 `/email-verification`。配套三处（都以"入口是不是 chatgpt.com 首页"为条件，`_CHATGPT_HOME_RE`，只认根路径、不认 `/auth/login_with`）：① 跳过 `chatgpt.com/auth/login_with` 预热（入口本身就是它，省一轮导航）；② 就绪判据用 `_LOGIN_READY_SELECTOR` 而不是 `_SIGNUP_READY_SELECTOR`——首页一开始**没有任何输入框**，只等输入框会白等满整个导航预算；③ `login_retry_click` 不再走"重新种会话"那套，而是像登录路径一样直接重跑 `submit_email`（该步开头就会点「Continue with email」，这正是弹窗默认手机号表单的正解）。有测试锁这条。
  - **MFA（`_run_mfa_enrollment`）**：`[data-testid="security-tab"]` → `[data-testid="mfa-authenticator-toggle"]` → 弹窗 `[aria-labelledby="enroll-totp-modal-title"]` → 「Trouble scanning?」→ `div[role="button"][aria-label="Copy code"]` 读 32 位 Base32 → `#totp_otp` 填 TOTP → 「Verify」。TOTP 用 `totp_auth.current_totp`，它自带 `min_valid_seconds=4`，天然满足需求"有效期小于 3 秒就等下一轮"。
  - **开 MFA 之前必须先吃掉「You're all set」欢迎弹层，而且要"等它出现"**：注册完成后 chatgpt.com 会 `showModal()` 一个**原生 `<dialog aria-label="You're all set">`**（只有一个 Continue 按钮），它把整页置为 **inert**——不点掉，后面每一次点击都点在空气上。它是 React 的 blocking initial modal，**在 load 之后才画出来，并且每次整页刷新都会重新弹一次**（导航到 `#settings/Security` 就是一次刷新）。踩坑实录（`logs/codex-a172c0e41e61c118718f0e3a-c0aedcf2-a1.log`）：`_dismiss_blocking_dialog` 只在被调用的那一瞬间看一眼，什么也没看到就走了（日志里连一行都没有），于是 MFA 全程点空，最后报成"点击「Authenticator app」后既没弹出 MFA 弹窗也没进入验证"，把真因藏了整整一轮。现在：① `dismissBlockingDialog(waitMs)` 支持**等待出现**并连点最多 3 层弹层，Python 侧两处调用（登录确认后、每轮 `#settings/Security` 导航后）都传 8s；② `open_mfa_enroll` 进来先 `unblock(8000)`，**每次 trustedClick 之前再清一遍**；③ 三处失败分支先查 `nativeBlockingDialog()`，弹层还在就如实报「欢迎弹层未能关闭，整页被 inert，点击全部落空」；④ `_dismiss_blocking_dialog` 现在"没弹层"也打一行 info——以前的沉默正是查不出问题的原因。`nativeBlockingDialog()` 只认原生 `<dialog>` 且**排除装着 `#totp_otp`/密码/邮箱框的弹层**（设置弹窗和 MFA 弹窗是 radix 的 `<div role="dialog">`，`settingsDialog()` 也显式排除 `DIALOG` 标签）。有测试锁"关弹层必须排在 `open_mfa_enroll` 之前且带等待窗口"。
  - **开 MFA 可能触发身份验证挑战**：页面被带离 chatgpt.com 去 `/email-verification`，此时点「Continue with password」→ `/log-in/password` → 填**刚生成的那个密码**。**注意这里要的正是 `/log-in/password`**，和注册阶段"绝不能走登录密码页"是相反的诉求（账号是我们自己刚建的，密码在手上），所以用独立的 `mfa_password_challenge` 动作，别去复用 `continue_with_password`（那个已经改成优先认 `/create-account/password` 的 href）。
  - **MFA 任何一步失败都要卡住等人工，绝不跳过账号**（2026-08-12 补）：密钥 OpenAI 只显示这一次，跳过去跑下一个 = 这个账号废掉。踩坑实录 `logs/codex-52b3d167068baf48cc943dff-5afab245-a1.log`：`open_mfa_enroll` 回了阶段 `error`，程序一句「打开 MFA 设置失败（阶段 error）」就把账号记成"已注册但未开启 2FA"走人了。原因是转人工只做在**密钥读取**那一步（`_reveal_totp_secret`），更早的失败（打不开设置页 / 身份验证没过 / 弹窗没出来）直接抛异常被外层吞掉。现在：`_run_mfa_enrollment` = 自动路径 `_open_mfa_dialog_and_reveal()` + **兜底转人工**，自动路径任何异常都进 `_wait_for_manual_totp_secret`（30 分钟，每 5s **只读不点**，人把密钥点出来就自动接上）；`open_mfa_enroll` 阶段不对也先自己重跑一轮（欢迎弹层可能又弹了）。唯一的提前出口是用户点「停止后续任务」。人工也没来时抛的错带 `_MFA_MANUAL_TIMEOUT_TOKEN`，外层据此**不再等第二个 30 分钟**。
  - **MFA 最终失败（人工也超时）不能把整单判失败**：走到这一步账号已经建成、租用号也已 `setStatus=3` 结清。只记成 `register_status='registered'` + "已注册但未开启 2FA：…"，密码照常保留，用户可事后手动补（清单里有「编辑」按钮），判成失败会让一个可用账号显示成"注册失败"。有测试锁这条。
  - **租用号的结算（setStatus）必须逐条写进日志**：`_settle_rental(logger, client, mail_id, status)` 统一收口，成功 INFO「smsbower 结算成功 mail_id=… setStatus=2（取消并退回）」、失败 WARNING「…可能仍在计费，请到后台手动关闭」+ notice、`ActivationGone` 记成"已不存在，视为已结清"。以前只在失败时打一行，日志里根本看不出"到底退没退"。覆盖面：① `_run_account_signup` 的 `finally`（流程内所有出口，成功=3 失败=2）；② 浏览器桥没连上、上游模块加载失败这类**还没进到那个函数**的失败，由 `_settle_mailbox_rental()` 退号；③ smsbower 客户端都建不起来（API Key 没配）时明确警告"无法结算，可能仍在计费"。`_settle_rental` 跑在 `finally` 里，所以它连未知异常都吞掉（只记一笔），绝不能盖掉账号本身的成败。
  - **注册失败一律不可重试**：租用号在收尾时已经 `setStatus=2` 退回，重试同一个账号必然在取码那步撞 `ActivationGone`。所以 `_run_account_signup` 的 `except` 会给**逃出去的所有异常**补上 `register_failed` 令牌（已有的 `account_already_registered` / `openai_risk_block` / `account_deactivated` 保持原样）——否则错误文案里只要恰好带个"超时"就会被 `_failure_info` 判成 `transient_network` 白跑一轮。有测试锁这条。
  - **smsbower 风控拦截账号直接清理**：`openai_risk_block` 且素材来源为 `smsbower_gmail` 时，`codex_service._handle_result_locked` 先保留 job 的失败状态和 `failure_code`（所以流水线失败计数、历史记录仍在），再从 `mailboxes.json` 删除账号记录；账号清单和后续导出不再出现这条废号。其它来源的账号不触发删除。
  - **取号并注册数量不设 50 上限**：`acquire-register-smsbower-gmail` 与旧的 `import-smsbower-gmail` 接口只校验数量大于 0，前端输入也不设置最大值；用户填写多少就由 `supply_count` 处理多少，仍保持一次只租一个号。
  - **Plus 免费资格**：注册成功后导航 `chatgpt.com/#pricing`，只读探测顶部有没有「Try Plus free for 1 month」（快照实证它是个 heading）。`probe_plus_offer` 返回 `true/false/**null**`——null 表示 pricing 视图压根没渲染出来，必须和"确实没优惠"分开，否则会把每个账号都误判成无资格。探测失败一律降级成"无资格"并 warning，绝不把已注册成功的账号判失败。
  - **导入区的按钮是「取号并注册 N 个账号」，一个一个来**：`POST /api/accounts/acquire-register-smsbower-gmail {count, confirmed:true}` 起一条 `mode='register'` 的流水线，**开跑时账号清单是空的**——`codex_service` 每处理完一个账号（成功/失败都算终态）才调一次 `supplier()` 租下一个号，注册完再租下下个，跑满 count 才收工。理由：号一租下来就开始计费，先把 N 个号全租好再排队注册，等于让排在后面的号白付租金；中途出问题也不会留下一堆没人管的号。
    - 实现：`start_batch(emails=[], mode='register', supplier=..., supply_count=N)`；`supplier` 是 webapp 里的 `_acquire_one_smsbower_gmail`（取 1 个 → `import_activations` 落库 → 返回 email），codex_service 对 smsbower 一无所知。`_supply_due()` 要求「没有在飞的任务 + 现有 job 全部终态 + 未暂停/停止 + 还欠号」；`_supply_next()` **在锁外面**调 supplier（网络几秒，别卡住整个面板），成功就 `_new_job_locked` 追加 job。
    - **两个间隔按触发原因严格分开**：首个账号立即取号；一个账号成功或失败产生终态结果、准备首次领取下一个账号时，设置 `next_supply_at`，按 `next_account_interval_seconds`（默认 60s，0-3600）等待。若这次取号失败，则清掉 `next_supply_at`，改为设置 `supply_retry_at`，只按 `supply_retry_seconds`（默认 10s，5-600）重试当前序号。后续连续取号失败始终走取号失败间隔，绝不能再次叠加“处理下一个账号”的间隔。
    - **取号失败绝不中断整批**（2026-08-13 改）：最常见的失败是库存空（`No mails yet`），过一会儿就有货，而中途收工等于把这一批剩下的计划全丢掉、还要人重新点一次。失败只记 `supply_error`/`supply_failures` 并把 `supply_retry_at` 推到 N 秒后，`_supply_due()` 见到未到点就返回 False——**主循环照常 0.5s 转，绝不在这里 sleep**，否则暂停/停止要等一个间隔才生效。间隔在插件「导入账号 → smsbower」表单里配（`supply_retry_seconds`，默认 10s，5-600），`codex_service` 对 smsbower 一无所知，所以由 webapp 读配置传进 `start_batch`。取号成功后要把 `supply_error`/`supply_failures`/`supply_retry_at` 清掉，别让界面一直挂着旧错误。**唯一的出口是用户点「停止后续任务」**（`_supply_due` 和收工判据都看 `stop_requested`），此时已注册好的账号一律保留。「正在取第 N 个」这条 notice 只在首次尝试时推，失败重试时只在第 1 次和之后每约 1 分钟推一次——每 10s 一条会把通知栏冲干净。有测试锁这两条。
    - 进度：`_pipeline_public_locked` 的 `total = 已建 job 数 + supply_remaining`，否则进度会先显示 1/1 再跳成 1/2，看着像重新开始。`supply_total`/`supply_acquired`/`supply_error` 透传给侧边栏显示。
    - 服务重启后 supplier 闭包没了，`_recover_interrupted` 顺手把 `supply_remaining` 清零，别让 UI 一直显示"还差几个"。
  - **纯取号入库的老接口还在**：`POST /api/accounts/import-smsbower-gmail {count}` 调 N 次 `getActivation` → `mailbox_store.import_activations()` 落 `source=smsbower_gmail` + `mail_id` + `register_status='pending'`，取到的号可以事后用「批量处理账号 → 注册账号」跑。两条路都以**已入库的 email 为 job 主键**（重试、`update_codex`、清单、导出都靠它），所以取号后必须先落库再建 job。
  - **`mail_id` 必须落盘**：后续无论成败都要 `setStatus`（2=取消 / 3=完成），丢了 id 就等于一直付租金。取号成功但入库失败时接口会 `release(mail_id, 2)` 逐个退回再报错——**别把这段"多余的"回滚删掉**。
  - **API 响应不是合法 JSON**：厂商文档给的样例是 `{"status":1"mail":"x","mailId":4,}`（少一个逗号、多一个尾逗号）。`smsbower_mail._parse_payload` 先严格 `json.loads`，失败再 `_repair_json` 修这两个已知缺陷，最后才退到字段正则。
  - **`{"status":1,"message":"Success"}` 是成功不是失败**：`setStatus` 成功也带 `message`，所以 `_request` **先判 `status==1` 再看 error/message**，顺序反了会把每次成功的取消当成报错。
  - 错误分类：`No mails yet`→`OutOfStock`（取号库存空，部分成功时保留已取到的并回 `warning`）、`Code has not been received yet`→`CodePending`（第二段轮询要靠它）、`Pass mail id` / `No activation found` / `already canceled`→`ActivationGone`。
  - **错误文案里绝不带 URL**：`api_key` 在 query string 里，多数 HTTP 库的异常文本会带完整 URL。配置读接口也只回 `api_key_masked`（`SmsbowerMailConfigStore.public()`），完整 key 只有 `get()` 内部用。
  - 配置存 `data/smsbower-mail.json`（`api_key` / `max_price` / `code_timeout_seconds` / `code_interval_seconds`），`api_key` 提交空值 = 保持原值（UI 只显示打码值，否则一保存就被清空）。`service=dr` / `domain=gmail.com` 写死，不做成配置项。
  - **取号后的账号 `otp_ready=False` 是对的**：它还没有密码和 2FA 密钥，`_otp_ready` 不放行 → 现有流水线不会误跑它；清单里显示「待注册」而不是红色的「登录素材未就绪」。
- **账号清单「编辑」按钮（人工补 MFA + 修正任务成败）**：只对 `smsbower_gmail` / `password_totp`（含 `origin_source` 是这两类的）显示，`_public().editable` 决定。点开小弹层可填 **登录密码 / 2FA 密钥（Base32）/ Plus 资格 / 最近任务状态** → `POST /api/accounts/{id}/credentials` → `mailbox_store.update_manual_credentials()`。密码 + 密钥齐了就走和注册成功**同一条改写路径**（`source=password_totp`、`import_material=email----密码----密钥`、`register_status=success`），导出格式完全一致。
  - **留空 = 保持原值**：清单接口**从不回传密钥**（只回 `has_totp_secret`），所以编辑框里的密钥永远是空的；不这么定就会"一保存把密钥清掉"。最近任务状态留空也是不修改。
  - **密钥格式当场校验**（`normalize_totp_secret`，非法直接 400）：手抄错一位写进去，账号看着"素材就绪"其实每次都算错验证码，比直接报错难查十倍。
  - **成功/失败编辑必须写 `codex_status`，不能写 `register_status`**：清单「最近任务」显示的是 `codex_status`；`register_status` 是 smsbower 注册内部进度，两者可能分别为 `success` 和 `failed`。编辑器只提供 `success` / `failed`，保存时同步写 `codex_message='已手动标记为…'`，因此保存后列表立即按新状态显示。后端仍兼容 `register_status` 参数，但不用于这个下拉框。
  - 账号正在流水线里跑时拒绝编辑（409），和删除接口一个判据。
- **账号清单「导出账号」按钮**：导出**已勾选**的账号（`selectedAccounts()`），没勾选时提示"请先勾选要导出的账号"——导出一律以勾选为准，不按筛选范围推断。复用已有的 `/api/accounts/selected/export`，按 source 分组打 zip。旁边批量条里的「导出原格式」是同一个能力，只是要先勾选才会出现。
- **账号导入格式**：`password_totp`（密码 + TOTP 2FA）同时接受 `email----密码----密钥` 和 `email|密码|密钥`，密钥后可再跟任意扩展字段（自动忽略，不算无效行）。解析在 `mailbox_store._split_password_totp`：先按 `----` 再按 `|` 拆，优先取固定三列，第三列不是合法 Base32 密钥时才回退"密码里含分隔符、密钥在最后一列"的老写法。导入提示 UI 只有插件侧边栏一套（`sidepanel.js`）。
- **调试页的「标签页绑定 + 快照」（在插件侧边栏）**：调试工作区列出所有窗口的标签页（`[id] title — url`），用户选一个绑定，再点「快照」把该页信息落盘。
  - 扩展消息：`list-tabs`（`chrome.tabs.query({})` + 注入可行性判断）、`capture-tab-snapshot`（带 `tabId`）。都在 `background.js` 的 `onMessage` 里，**不经过 Python 桥接**，也没改 `getActiveTab()` / `performBridgeRequest`，OAuth 流水线路径零影响。
  - 快照走 `executeInIsolatedWorld(tabId, collectPageSnapshot)`：**`executeScript` 不要求目标 tab 是 active**，所以绑定的后台 tab 也能取，不抢焦点。`collectPageSnapshot` 必须自包含（会被序列化，闭包变量取不到）。
  - 快照内容面向"后续怎么操作这个页面"：page(url/title/readyState/viewport…) + 可操作元素清单（每个带 **`selector`（querySelector 可直接用）** 和 `xpath`、rect、visible、label、value）+ forms（含 submit 控件清单）+ iframes + **open shadow DOM 递归**（现代页面控件常在 shadow root 里，不递归会看着像空页）+ errors + storage keys + cookie names + 完整 HTML。password 类型的 value 一律脱敏成 `***(N 位)` 再落盘。
  - 落盘：`POST /api/extension/debug/snapshot` → `logs/debug/{tabId}-{YYYYmmdd-HHMMSS}-screenshot.log`（UTC，JSON 文本；同秒第二次自动加序号不覆盖）。`artifact_store.list_logs` 递归扫 `*.log`，所以快照会出现在「日志」页且可下载。
  - 绑定的 tabId 存 `chrome.storage.session`（不是全局变量：侧边栏一关一开脚本就重跑；tabId 也只在本次浏览器运行内有效，生命周期正好对上）。绑定的 tab 被关掉时列表刷新会清空绑定并提示，不会静默改打别的 tab。
  - `chrome://` / 应用商店 / `file://` 等页面无法注入，列表里标灰禁选；无痕窗口的标签页要用户在 `chrome://extensions` 给扩展开「允许在无痕模式下运行」才会出现在列表里。
  - **坑：后台 tab 的 timer 被 Chrome 节流**。快照是一次性注入，不受影响；但如果以后要在绑定 tab 上做"等元素出现"的轮询，页面里的 `setTimeout` 轮询在非可见 tab 会被降到分钟级——要么让两个 tab 各自在可见窗口，要么把轮询放到 service worker 侧、每次注入只做一次性判断。
- **gcash 提炼（`mode:'gcash'`，两个绑定标签页）**：插件「批量处理账号」的「gcash提炼」按钮（在「仅登录」后面）→ `start_batch(mode='gcash')` 从 `data/gcash-tabs.json`（`GcashTabStore`，调试页绑定后保存）取两个 tabId 塞进 mailbox → worker `run_codex_only` 读 `gcash_extract` flag 走 `_run_gcash_extraction`。**不接码、不走 OAuth**，但流程带人工：每个账号拿到付款链接后要人扫码。
  - **流程**：`_bridge_tab(login_tab)` 上下文里复用 `_login_account_in_browser`（线程局部 `_bridge_context.tab_id`，`_bridge_request` 自动补 `tab_id`，全部旧 helper 零改动）→ `_read_gcash_access_token`（登录确认时已拿到 session payload，取 `accessToken`，没有就再 `page_fetch`/打开 session 接口兜底）→ 切 `_bridge_tab(extract_tab)`：`gcash_probe` 探测（`page_ready`）→ `gcash_submit`（填 `#token` + 点 `#submitButton`）→ `_wait_for_gcash_outcome` 轮询判定 → 成功则 `_bridge_navigate(链接)` 回登录 tab，`_wait_for_gcash_scan` 监控 `tab_url` 直到离开 `m.gcash.com`/adyen 跳回 `chatgpt.com`。
  - **成败判定（快照实证）**：进度条 `#progressBar` 100% 为前提；`#resultPanel` **可见** 且有链接 = 成功，不可见 = 失败。**失败页的 `#resultValue` 里残留着上一轮的旧链接**（panel `hidden=""` 藏起来了），所以绝不能直接读 value 判成败。页面轮询 `setTimeout` 在后台 tab 会被节流，判定放 service worker 侧驱动。
  - **登录前清理只清登录 tab**：`_login_account_in_browser` 的 `_bridge_cleanup` 因 tab 上下文自动钉在登录 tab；前端 `startPipelineRun('gcash')` 启动前**跳过** `runBrowserCleanup`（它只认活动页，会把绑定的 153 提炼页打成 about:blank），`trackCompletedJobs` 对 `mode==='login'|'gcash'` 同样跳过收尾清理。
  - **失败语义**：提炼失败 / 扫码超时 / 链接为空 → 消息带 `gcash_extract_failed` 令牌 → `_failure_info` 分类 `gcash_extract_failed`（**不可重试**，必须排在 `超时` 规则之前）；标签页失效错误也补打该令牌。提炼失败会 `store.update_gcash(status='failed')` 落邮箱记录。
  - **导出**：账号清单「导出 gcash 提炼结果」按钮（`+导入账号` 后面）→ `POST /api/accounts/gcash-export`（`confirmed` + `account_ids`，**取当前筛选+搜索可见的账号**，有结果才导出）→ 按 `----提炼成功----` / `----提炼失败----` 分组，每行 = 原导入素材 + `----` + accessToken（`mailbox_store.export_gcash`）。账号列表 API 只暴露 `gcash_status`/`has_gcash_token`/`gcash_message`，token 不出口。
- **`vendor/turb-gpt-free-register/core/generic_api_mail_client.py`** — "通用 API 取码邮箱"客户端(轮询取码地址提取 6 位 OTP)。
- **代理池（插件「代理」页）**：`src/proxy_store.py`（`data/proxies.json`：`enabled` 全局开关 + `cursor` 轮询游标 + 代理列表）+ `src/proxy_tester.py`（测可用性/出口 IP/地理位置/延迟）+ `webapp` 的 `/api/proxies*` 一组接口。
  - **按账号轮询**：`codex_service.start_batch` 给每个账号调一次 `proxy_store.next_for_account()`（跳过禁用项、游标持久化跨批次续着走），把 `ProxyStore.browser_config()` 的结果塞进 mailbox 的 `proxy` 字段；worker 在 `_login_account_in_browser` / `_run_codex_in_browser` 的 **`_bridge_cleanup()` 之前**调 `_bridge_apply_proxy(mailbox)`（桥接 kind `proxy_apply`）。**没分配到代理也要发 `proxy:null`**，否则上一个账号的代理会被下一个账号继承。设代理失败只 warning 不中断任务。
  - **浏览器侧**：`background.js` 的 `applyProxyConfig` 用 `chrome.proxy.settings.set` 的 `fixed_servers` 模式（manifest 加了 `proxy`/`webRequest`/`webRequestAuthProvider` 权限）。`bypassList` 必须包含 `<local>`/`localhost`/`127.0.0.1`/`[::1]`——后端在回环上，走代理会直接把桥接打死。带用户名密码的代理靠 `chrome.webRequest.onAuthRequired`（`asyncBlocking`，只应答 `details.isProxy`）填凭证，凭证存 `chrome.storage.session` 以便 SW 休眠重启后仍能应答。
  - **坑**：① `chrome.proxy` 是**浏览器全局**设置，一旦设上会一直生效（普通浏览也走代理）直到下个账号换代理或用户关掉代理池——所以代理页底部常驻显示"浏览器当前正在使用哪个代理"。② 别的代理类扩展会抢占该设置，`set()` 还会假装成功；`proxyControlBlocker()` 先读 `levelOfControl`，`controlled_by_other_extensions` / `not_controllable` 直接报错。③ **SOCKS 代理的用户名密码在 Chrome 里用不了**（SOCKS 没有 407 挑战），有 auth 的 socks 代理会回 `auth_unsupported` 警告，要改用 http(s) 代理或 IP 白名单。
  - **接受的代理写法**（`parse_proxy_url`）：协议只认 `http` / `https` / `socks5`（不写默认 http，正好也是 `chrome.proxy` fixed_servers 的 scheme 名，无需转换）；地址部分只接受 `host:port` 和 `user:pass@host:port` 两种。密码里可以有 `@`（按**最后一个** `@` 拆分）。`host:port:user:pass` 这类冒号简写**不支持**，会当无效行报出来而不是被读成怪端口。
  - **凭证不出口**：API 只返回打码后的 `url_masked`（`http://***:***@pro***.example.io:8001`），完整 URL 和密码只在后端 `get_raw()` 内部用于测试和下发给浏览器。
  - **别让浏览器自己出卖“我在用代理”**（`applyProxyStealth`）：**WebRTC** 默认会把代理背后的真实地址通过 ICE candidate 交出去，这是最常见的代理识别手段——设代理时把 `chrome.privacy.network.webRTCIPHandlingPolicy` 设成 `disable_non_proxied_udp`，撤代理时 `clear()` 还原。**注意：这只是不再白送信号，改不了出口 IP 本身的信誉——机房 IP 段被 Cloudflare 标记了就是标记了，只能换住宅代理。**
  - **时区/语言已不再跟着代理走**（历史实现 `applyProxyGeoOverrides` / `PROXY_ACCEPT_LANGUAGE` 已删除），改由用户在「调试 → 指纹」显式指定、浏览器全局生效，见下面的「浏览器指纹」条目。代理测出的 `country_code`/`timezone` 仍然保留，但只作参考显示（`proxy_apply` 回包里的 `proxy_timezone`），不再自动改浏览器。
  - **可自查**：代理页「检查当前页指纹」按钮（`inspect-fingerprint` 消息）读回**页面真实看到的**时区/偏移/语言/`navigator.webdriver`，与「指纹」页设定值并排显示——“已对齐”是能验证的事实，不是口头保证。
  - **走代理时不要抢在风控脚本前面动手**：登录页会加载 Cloudflare JS 检测（`/cdn-cgi/challenge-platform/.../jsd/main.js`）和 OpenAI sentinel iframe，它们要几秒钟在后台打分。为"提速"而做的 `ready_selector`（一看到邮箱框就返回）在直连没问题，走代理时等于抢跑，会被弹回「你的会话已结束」。所以 `_login_account_in_browser` 判断 `mailbox["proxy"]` 存在时**改为等真正的页面加载完 + 静置 `_PROXY_SETTLE_SECONDS`(3s)**，直连仍走快路径。**教训：给自动化提速和绕过风控是互相矛盾的，加速前先问这一步是不是正被人打分。**
- **所有点击/按键/输入必须是 CDP 可信事件（`isTrusted:true`）**。合成事件（`node.dispatchEvent(new MouseEvent('click'))`、`node.click()`、`form.requestSubmit()`、合成 `KeyboardEvent`）的 `event.isTrusted` 恒为 `false`，风控一行代码就能识别——实测表现为**页面停在 `/email-verification` 提交无效**。
  - **输入同样要可信，`fillValue` 不算**。`fillValue` 是"原生 setter + 合成 input/change"，事件 `isTrusted:false`，和合成点击是同一个破绽。**注入层已全面改用 `trustedFill(node, value)`（先可信点击聚焦 → CDP 选中全部 → Delete → 逐字符真实按键）和 `trustedType(value)`（往已聚焦元素打字，供 OTP 分格框自动跳格用）**；`trustedFill` 打完会核对 `node.value`，React 受控组件吞键会直接报错而不是静默提交空值。逐字符用 `Input.dispatchKeyEvent`（keyDown+text+keyUp）而**不是 `Input.insertText`**——后者等价于输入法上屏，不产生按键事件，按键节奏类打分能看出来；字符间隔 18~63ms 随机。
  - **自查**：`grep -n "fillValue(" chrome_plus_ver/background.js` 在**注入层（约 2200 行以后）应当为空**。仅剩 gcash 提炼页（`#token`，第三方本地工具页、无风控）还在用 `fillValue`。
  - **实现（`startTrustedClickPump`）**：页面步骤是一整个注入函数，点击/输入发生在函数内部、没法"返回"给 service worker。所以注入侧的 `trustedClick`/`trustedKey`/`trustedFill` 都是把目标的**视口坐标 / 键名 / 文本**推进 `window[__cdpc_xxxx]` 这个每步随机命名的通道并等待回执；SW 侧的 pump **在该步仍在执行时**并发注入读取队列，用 `Input.dispatchMouseEvent` / `Input.dispatchKeyEvent` 发真实事件，再写回 `done`。整步共用**一个** debugger 会话（`attachDebuggerOnce`），避免每次操作 attach/detach 闪 infobar；步骤结束时删掉 `window` 上的通道不留痕迹。
  - **绝不回退到合成操作**：CDP 附加失败时 `trustedClick` / `trustedFill` 直接抛错（提示"该页是否开着 DevTools"），而不是偷偷用合成事件——否则就会出现"看起来在跑、其实每一步都被标记"的情况。
  - **`form.requestSubmit()` 和合成 Enter 同样不可信**，已全部换成 `trustedKey('Enter')`（真实按键才会触发表单默认提交）。gcash 的「开始提炼」按钮也改成注入只回报坐标、由 `runGcashAction` 调 `clickTrustedPoint` 点击。
  - **加新交互时的铁律**：注入代码里**只许定位元素、读状态**，任何"触发"和"输入"动作都要走 `trustedClick`/`trustedKey`/`trustedFill`。改完用 `grep -n "dispatchEvent(new MouseEvent\|\.click()\|requestSubmit()" chrome_plus_ver/background.js` 自查，应当为空。
- **浏览器侧超时「通用配置」（插件「调试」页第一节）**：`src/browser_config_store.py`（`data/browser-config.json`）+ `/api/browser-config` GET/POST。两个值：`page_load_timeout_ms`（导航等页面加载完，默认 45s）、`element_wait_timeout_ms`（页面里等元素出现，默认 30s）。
  - **怎么送到浏览器**：扩展读不到后端配置文件，所以 `_bridge_request` 在每个 `navigate` payload 里塞 `page_load_timeout_ms`、每个 `page_action` payload 里塞 `element_wait_timeout_ms`；`background.js` 的 `navigateActiveTab` 和注入函数里的 `waitFor` 读这两个值。**改完立即生效，不用重载扩展**（下一个桥接请求就带新值）。
  - **元素等待是「基准值」不是单点值**：注入函数里 19 处 `waitFor` 大多传了显式毫秒数，`waitFor` 用 `WAIT_SCALE = 配置值/30000` 对**所有**等待按同一比例放大，避免只有省略参数的那几处变长、其它照旧。
  - **后端超时自动推导，永远比浏览器长**（`navigate_bridge_timeout_ms` = 页面加载 + 45s；`page_action_bridge_timeout_ms` = 元素等待 × 3 + 60s）。× 3 是因为 `submit_email` 一次调用最多花三轮元素等待（点登录链接 → 等邮箱框 → 等提交后落地）。**别把这两个改成固定值**，否则用户一调大就会撞上踩坑 #2 的"两端同时超时 → 孤儿卡死"。
- **浏览器指纹「时区 / 语言」（插件「调试」页第二节）**：用户显式指定，**浏览器全局生效**（地址栏导航、页面里的 fetch/XHR 全算），带一个开关。存两份：后端 `data/browser-config.json` 的 `fingerprint` 段（`{enabled,timezone,language}`，同一个 `/api/browser-config` GET/POST，`BrowserConfigStore.fingerprint()`）是「配置」；扩展 `chrome.storage.local` 里那份是「还在生效的那份」——侧边栏关掉后 service worker 仍要靠它给新标签页挂覆盖。侧边栏打开调试页 / 点保存时把后端那份 `set-fingerprint` 推下去对齐。
  - **实现仍是 CDP，不打 JS 补丁**：`applyFingerprintToTab` 对每个标签页 attach debugger 后发 `Emulation.setTimezoneOverride` + `Emulation.setLocaleOverride` + `Emulation.setUserAgentOverride`（UA 保持真实，只带 `acceptLanguage`，并回填真实的 `userAgentMetadata`，避免 UA 与 Sec-CH-UA 自相矛盾）。**为什么不改写 `Intl`/`Date`**：打过补丁的函数用 `Function#toString()` 一查就露，CDP 是让引擎本身改口径。时区**不在任何 HTTP 头里**，只能这么改；Accept-Language 额外用 `declarativeNetRequest` 会话规则（全站，不再只限 openai 域名）兜底，覆盖那些没挂上会话的请求。
  - **覆盖只在 debugger 附加期间有效**（能跨导航）。所以：开启时给所有可注入标签页挂上，`tabs.onCreated`/`onUpdated(loading)` 给新页补挂，`debugger.onDetach` 里补回（`canceled_by_user` = 用户开了 DevTools，不跟他抢；`target_closed` 直接忘掉），**service worker 每次唤醒都重挂一遍**（SW 重启会带走会话）。代价是每个标签页顶部常驻「正在调试此浏览器」提示条。
  - **和流水线的 debugger 会话共存**：`withDebuggerSession` / `attachDebuggerOnce` 先看 `fingerprintTabs.has(tabId)`，是则**复用且绝不 detach**（detach 会把时区/语言覆盖一起带走）；反过来若点击泵先 attach、指纹借用了它的会话，泵 release 后调 `reapplyFingerprintAfterRelease` 重挂。改这两个 helper 时务必保持这条。
  - **踩坑（血的教训，改语言前必读）**：曾按代理国家自动发 `ja-JP`，OpenAI 直接返回**日文界面**（标题 `お帰りなさい`），而本仓库所有阶段判据（`/verification|验证码|otp/`、`会话已结束`…）只认中英文 → 判成 `unknown` → 把一次**验证码其实已发出**的流程杀掉了。所以 UI 明确建议填 `en-US`，判据里也补了日/韩/西/法/德关键词兜底。**任何"让页面本地化"的改动都会连带打断文本判据，改语言/地区前先想清楚谁在读这些文案。**
  - **不瞎猜**：启用必须同时填时区和语言（后端 `_validate_fingerprint` 校验 IANA 时区名 + BCP-47 标签，本机装了 tzdata 时还查真实时区表；没有 tzdata 就只查格式——全拒比放行一个格式正确的名字更糟）。时区列表在 UI 侧由 `Intl.supportedValuesOf('timeZone')` 直接给出（Chrome 99+），老内核用内置兜底表。
- **仅登录模式（`mode:'login'`）**：插件「仅登录」按钮（在「导出 Session」之后）→ `start_batch(mode='login')` 给 mailbox 打 `login_only=True` → `run_codex_only` 走 `_run_login_only`：与 Session 导出共用 `_login_account_in_browser`（清浏览器 → `chatgpt.com/auth/login_with` → submit_email + 邮箱 OTP，或密码 + TOTP），登录完成后**不再做任何导航**直接返回，**不读 session、不走 OAuth、不接码**。后端强制一次只允许 1 个账号（`start_batch` 校验），UI 也只在恰好 1 个可运行账号时启用按钮。
  - **启用代理时只走 `https://chatgpt.com/`（`_PROXY_LOGIN_ENTRY_URLS`）**。走代理时 auth.openai.com 那两个入口最终都会被弹回「你的会话已结束」，而所有路径本来就汇流到 chatgpt.com，先试别的只是每个白烧一轮。
  - **chatgpt.com 可能永远不报 `load complete`，别拿它当前置条件**。它是长连接 SPA，走代理时标签页会一直停在 `loading`，而页头（含 Log in 按钮）其实早就能用了——死等加载完的结果是无限循环 `导航超时（https://chatgpt.com/）…重试第 N 次：标签页加载超时`。现在登录入口的导航：① 带 `ready_selector`（有任何可点控件就返回）；② `tolerate_timeout=True`，超时只 warning 不抛错、也不重发导航；③ 代理下仍保留 `_PROXY_SETTLE_SECONDS` 静置给风控脚本打分。**真正判断"能不能往下走"的是 `submit_email` 自己的元素等待，不是 load 事件。**
  - **注入侧同理：不要因为"第一眼没看到控件"就放弃**。前置循环每轮先 `waitFor(邮箱框 || Continue with email || Log in)`（首轮 30s、之后 10s），拿到再点，最多 8 轮，并用 `lastClicked` 防止对同一个控件反复点。
  - **chatgpt.com 的登录是两步点击，缺一不可**：① 右上角「Log in」→ ② 弹出 **“Log in or sign up”** 对话框，**默认是手机号登录、根本没有邮箱输入框**，必须点 **「Continue with email」** 才会出现邮箱框。`submit_email` 的前置循环因此按"先找 emailSwitch、再找 loginLink"的顺序最多跑 6 轮，两种控件任一出现都算这一轮有进展。
  - **坑：`LOGIN_LABEL_RE` 绝不能匹配光秃秃的「Continue / 继续」**。那个对话框底部的「Continue」是**提交手机号**的按钮，一旦被当成登录入口点下去，就等于提交了一个空手机号。判据只认 `^(登录|登入|登陆|log in|sign in)$`，「Continue with email」由单独的 `emailSwitchControl()` 匹配（并排除含 phone/手机/电话 的标签）。
  - **坑（吃了大亏）：可见性判断绝不能用 `offsetParent !== null`**。CSSOM 规定 **`position:fixed` 元素的 `offsetParent` 恒为 `null`**，而弹窗几乎都是 fixed —— 于是「Log in or sign up」对话框里的**每一个按钮**（包括 `Continue with email`）在判据眼里都"不可见"，`emailSwitchControl()` 永远返回 null，流程点开弹窗后就卡死。注入函数里 3 处 `isVisible` 已全部改为：`isConnected` + `!disabled` + `getBoundingClientRect()` 有尺寸 + `getComputedStyle` 的 `visibility/display/opacity` 检查。**新写任何元素查找都别再用 `offsetParent`。**
  - **登录入口顺序（`_LOGIN_ENTRY_URLS`）**：`auth.openai.com/log-in?usernameKind=email` → `chatgpt.com/` → `chatgpt.com/auth/login_with`。**`?usernameKind=email` 不能省**——`/log-in` 某些情况下默认成**手机号登录表单**，页面上压根没有邮箱框，流程会白等满一个元素等待预算然后报"未找到邮箱输入框"。OAuth 路径里那次 `/log-in` 导航也用同一个常量 `_OPENAI_LOGIN_URL`。**别再把 login_with 放第一位**——它是重型 SPA 路由，有相当概率渲染成登出态聊天落地页而不重定向，白费一整轮。
  - **兜底：页面已经是手机号表单时自己切回邮箱**。经「你的会话已结束」的登录链接进来是不带参数的，`submit_email` 检测到"有 tel 输入框且没有邮箱框"就点「改用邮箱登录 / Use email instead」（排除含"手机/电话/phone"的标签，避免反向点回去）。
  - **导航不等整页加载完**：`_bridge_navigate(..., ready_selector=_LOGIN_READY_SELECTOR)` 把"看到邮箱框/密码框/登录链接就够了"下发给扩展，`waitForTabReady` 一边等 `status==="complete"`、一边每 200ms 用 ISOLATED world 探测该选择器，哪个先到就返回。`status==="complete"` 要等所有子资源，走代理时比表单可用晚好几秒，这就是"输入邮箱要等半天"的来源（`fillValue` 本身是瞬时写值，不是逐字打字）。导航后的固定 `time.sleep(2.0)` 也已删除。
  - **坑：ready 探测要防"旧文档误命中"**。重新导航时旧页面上可能还留着邮箱框，导航未提交就探测会立刻"成功"，结果注入到一个马上要被销毁的文档里。`waitForTabReady` 用 `ignoreUrl`（导航前的 URL）做门槛，URL 没变就不采信探测结果。
  - **踩坑：仅登录必须跳过"任务完成后清理"**。`sidepanel.js` 的 `trackCompletedJobs` 见到 job 变终态就调 `runBrowserCleanup`，而 `background.js` 的 `cleanupBrowserState` 会清 cookies 并把标签页 `chrome.tabs.update(url:'about:blank')` —— 对仅登录来说等于刚登录就被登出、页面变 about:blank。修复：`start_batch` 把 `mode` 写进 pipeline（`_pipeline_public_locked` 直接透传），前端 `pipelineState.mode==='login'` 时跳过收尾清理；**启动前**的清理照常执行。
  - 登录成功后也不要再 `_bridge_navigate("https://chatgpt.com/")`：页面本来就在登录后的跳转链上，多这一次导航只会撞"标签页加载超时"，并和标签页被停到 about:blank 抢时序。
  - **帧销毁 = 登录成功（本轮实测：密码+TOTP 提交后必现 `Frame with ID 0 was removed`）**：登录（非 OAuth）路径里 OTP/密码提交成功后页面直接跳 chatgpt.com，注入帧被销毁，`executeScript` 抛 `Frame with ID 0 was removed`。绝不能当失败。`_login_account_in_browser` 用 `_submit_login_step`（`_is_frame_teardown_error` 识别帧销毁类错误：`Frame with ID X was removed` / `No frame with id` / `No tab with id` / `Target closed` / `context was destroyed` / `页面动作返回空结果`，返回 `frame_teardown` 标记继续）之后一律用 `_confirm_logged_in` 做**唯一判定**：轮询 `chatgpt.com/api/auth/session`（先原页读，读不到/未登录再导航到 chatgpt.com 后同源读，90s 预算），返回有 `user`/`accessToken` 的 JSON 才算登录成功；超时且发生过帧销毁则报"提交后页面跳转但未确认登录态"。`submit_email` 的 5 次重试仅针对它自己（导航链未稳时帧销毁），其它步骤不再盲目重试（会重复消费一次性 TOTP）。Session 导出复用同一判定：`_confirm_logged_in` 拿到的 payload 直接 `_save_session_payload` 落盘，不再二次读取。
- **`tests/`** — pytest；改动后跑 `python -m pytest -q`（当前基线 **334 passed**）。

## OAuth 流程阶段（OpenAI 新注册流）

`email → create-account/password(此时还没发 OTP) → activate_passwordless_signup(或设密码) → 邮箱 OTP → phone-verification → /about-you(填姓名+年龄) → /sign-in-with-chatgpt/codex/consent(点"继续") → localhost:1455/auth/callback?code=...`

- **手机验证两个 URL 要分清（重要）**：`https://auth.openai.com/add-phone` = 手机**号码输入**页；`https://auth.openai.com/phone-verification` = 收到号码后的**验证码输入**页。一个号码超时后，OpenAI 会把它**锁定在 /phone-verification 的验证码步骤**，重新打开 /phone-verification 只会显示同一个锁定号码——要换号必须导航到 **/add-phone** 拿到新的号码输入框。接码重试循环里 attempt>1 会先 `_bridge_navigate("https://auth.openai.com/add-phone")` 再取号，否则新号会落在旧号的验证码页上被白白浪费。
- **清 cookie 后 `auth.openai.com/log-in` 是"会话已结束"过渡页**（`is_missing_session:true`），只有一个"登录"链接（`href=chatgpt.com/auth/login_with?callback_path=/`），**没有邮箱输入框**。要进真正的邮箱登录页必须点这个链接 / 直接导航 `chatgpt.com/auth/login_with`。所以 Session 导出的登录入口用 `chatgpt.com/auth/login_with`（不是 `/log-in`）；`submit_email` 也加了兜底：没有邮箱框但有"登录"链接时先点它再等邮箱框（OAuth 流程走 authorize URL 会自建 session，不撞这个过渡页）。
  - `/auth/login_with` 是**重定向端点**，会连跳好几跳才落到邮箱表单；`submit_email` 可能注入到正在被销毁的帧里报 `Frame with ID X was removed`。`_run_session_export` 对 submit_email 做了 Python 侧重试（帧销毁/超时 → 等 2s 重试，最多 5 次），等重定向链稳定后邮箱表单就在了。
  - **登录页的邮箱框和"继续使用 Google/Microsoft/Apple"社交按钮在同一个 form 里**。`submitNearestForm()` 会点该 form 里**第一个** submit 控件，很可能就是 Google 按钮 → 页面跳去 Google 登录、OTP 邮件根本没发出，最后表现为"等待通用 API 验证码超时；HTTP 200 但未提取到验证码"（看着像取码邮箱坏了，其实是点错按钮）。`submit_email` 现在显式挑选邮箱的"继续/Continue"控件并**排除**社交 provider（google/microsoft/apple/facebook/github/sso/passkey/手机…），挑不到才回退按 Enter；并且若提交后落到第三方登录域名，直接报错而不是谎报"OTP 已发送"。

- **老账号重新走 OAuth 不需要接码**：只有官方真的跳到手机验证页时才接码。用 `probe_stage`(无副作用探测)判断 `needs_phone`，避免浪费短信号码。
- `background.js` 的 `executePageStep` 处理 `page_action`（submit_email / submit_email_otp / submit_phone / submit_phone_otp / activate_passwordless_signup / finalize_and_get_callback / probe_stage / snapshot_dom）。`navigateActiveTab` 单独处理 `navigate`。

## 接码（SMS）架构

- **多渠道 provider**：`src/hero_sms.py` 的 `_RuntimeSmsCoordinator` 在上游 SMS 生命周期模块上加一层，支持 `hero` + `smsbower` 两个渠道。渠道顺序由 `SMS_CHANNEL_PRIORITY` 决定（默认 hero）。`acquire_number` 按渠道优先级依次尝试；单渠道内按国家队列取号。
- **国家 cursor（hero 与 smsbower 对称）**：每个渠道各有一个 `_country_cursors[channel]`，取号成功后 `_advance_cursor` 前进一位，下次从下个国家开始（跨任务轮转，避免总卡同一国家）。指定了具体 country 时钉住该国不轮转。
- **无“最多换号次数”**：接码失败后按优先级把**所有渠道×国家组合**逐个试完才报失败。浏览器路径 `upstream_bridge._sms_slot_count()` 算出组合总数作为循环上限；`SMS_MAX_RETRIES` env 仅为 vendor 协议路径保留，保存时按组合数自动写入（不再是用户可填字段）。真无号（`SmsNoNumbersError`）时提前 break。
- **每次失败必取消**：每条失败路径都调用 `sms_provider.cancel()`（按渠道路由发 `setStatus=8`，过 2 分钟 EARLY_CANCEL 窗口后生效），避免号码后续再收短信被扣费。改重试循环时保证新增的失败分支也 cancel。
- **限价按国家生效（每国必填）**：不再有渠道级全局限价。Hero 每国 `{max, fixed}`（fixedPrice 默认 false）；smsbower 每国 `{min?, max}`。存储在 env `HERO_SMS_COUNTRY_PRICES` / `SMSBOWER_COUNTRY_PRICES`（JSON, 按国家 id）。`sms_config.normalize_country_prices` 校验每个选中国家必须有 max；`hero_sms._country_price_map` 在 acquire 时按国家覆盖价格参数。保存时缺任一国家的 maxPrice 会报错。
- **UI（只有一套）**：**Chrome 扩展侧边栏** `chrome_plus_ver/sidepanel.html` + `sidepanel.js`（含 hero + smsbower 两个渠道配置），打 `/api/sms-config`。Flask 网页版 UI（原 `templates/index.html`）已删除，后端只保留 JSON API（`/` 返回提示 JSON）。国家选择用共享的 `createCountryPicker` 工厂（`priceMode:'hero'|'smsbower'`），每个国家行内嵌限价输入：hero=价格上限+精准价(fixedPrice)，smsbower=最低价+价格上限；状态在 picker 的 `S.prices`，`getPrices()/setPrices()` 存取。已移除全局 `#heroMaxPrice`/`#smsMaxRetries`/`#smsbowerMinPrice`/`#smsbowerMaxPrice`。
- 两份接码平台 API 文档在 `C:\vs_project\my_register\web-workflow-reconsitution\doc\`（hero-sms-api.md、smsbower-api.md）。smsbower 取消用 `setStatus&status=8`；hero 用 `setStatus&status=8`。hero 取号 `getNumberV2` 支持 `maxPrice` + `fixedPrice`。

## 关键技术约定

- **改扩展 JS(`background.js`/`sidepanel.js`)→ 必须去 `chrome://extensions` 重载扩展**（sidepanel.js 改动还要关掉侧边栏重开一次，让它重新抓 windowId）。
- **改 Python → 必须重启 worker/服务进程**（接码智能门控、取码逻辑都在 Python 侧；只重载扩展无效）。
- 页面注入代码（`executeInMainWorld` 里那个大 `async (input)=>{...}` 箭头函数）在页面 MAIN world 运行，用 `fillValue`(原生 setter + input/change，兼容 React)、`trustedClick`(合成 pointer/mouse 事件)、`waitFor(predicate, timeoutMs)`。
- **活动页锁定到侧边栏所在窗口**：不要用 `chrome.tabs.query({active:true, currentWindow:true})`（在 SW 里跟随焦点窗口）。侧边栏用 `chrome.windows.getCurrent()` 拿 windowId，随每条桥接消息带上；后台 `getActiveTab()` 优先按 `boundWindowId` 查，窗口没了才回退 currentWindow。隐私窗口跑任务、切到普通窗口不会被劫持。
- **每个账号开始前必须清浏览器（隐私模式）**：串行流水线切下一个账号时，上一个账号的 Cookies/会话/consent 不能残留，否则 OpenAI 会显示 `/choose-an-account` 或 `consent verifier already used`。侧边栏的"任务完成后清理"是**轮询驱动的、有时序竞态**（下一个 worker 可能已经开始 OAuth）。可靠做法：worker 在 `_run_codex_in_browser` 首个 `_bridge_navigate` **之前**同步调用 `_bridge_cleanup()`（桥接 `kind:'cleanup'` → `background.js` 的 `cleanupBrowserState`，清 openai/chatgpt 的 cookies/cache/localStorage/indexedDB/serviceWorkers）。

## 踩坑经验（重要，避免重复犯）

1. **finalize 帧销毁 = OAuth 其实成功**。收尾时页面从 auth.openai.com 连跳到 localhost callback，注入帧被销毁有两种表现：抛 `Frame with ID X was removed`，**或 `executeScript` 静默返回 null → "页面动作返回空结果"**。两种都要恢复：轮询标签页自身 URL 拿 callback；没到就等下一页重新注入 finalize 继续驱动，直到拿到 callback 或超时。别只处理抛异常那一种。
2. **finalize 超时预算要 < Python 请求超时**，否则两个 180s 撞车 → 孤儿卡死（看起来像"崩溃"）。现在浏览器侧 120s 返回、Python 侧 180s 收，留 60s 余量；超时返回带 `snapshot: buildDebugSnapshot()`，Python 会打 `[BridgeDOM]` 卡在哪个页面。
3. **consent 页的"继续"只能点一次**（consent verifier 是一次性的）。finalize 收尾循环原来每 500ms 点一次主按钮，consent 页第一次点击已消费 verifier 并跳到 callback（带 code），但导航还没完成时循环又点了一次 → 第二次提交复用已花掉的 verifier → callback 变成 `access_denied: The consent verifier has already been used`。修复：finalize 循环对每个页面 URL **最多提交一次**（`submittedPages` 去重），点击后 `waitFor` 等 URL 变化/跳到 callback，不再狂点。
3. **OTP 误判成功（邮箱 & 手机同类坑）**：email-verification / phone-verification 页永远有"继续"按钮，且错/过期码只会在网络往返后才渲染错误（甚至只是静默清空输入框）。若在"仍停留在该验证页"时就当作通过，会白白浪费短信号码、并让 finalize 卡在空验证码框上狂点"继续"报"需要填写验证码"直到超时。
   - 邮箱：`onEmailOtpStep`(有 code 输入框 + 在 /email-verification) 守卫，还在该步只看 `visibleError()`，绝不当 post-otp。
   - 手机：`submit_phone_otp` 必须确认页面**真的离开了 /phone-verification**(跳到 about-you / consent / callback / 无 code 输入框)才算通过；否则回传 `error_text`(可为合成"手机验证码未通过：页面仍停留在 phone-verification")，让 Python 接码循环 cancel 当前号码、换新号重试，**绝不 log "手机号验证通过"**。SMS 码被 OpenAI 拒(号码被标记/码过期)时错误文案可能是"需要填写验证码"(必填)而非"代码不正确"。
4. **一次性 OTP 严禁复用旧码**。快速/背靠背重试时取码接口常把上一轮旧邮件的验证码当成本轮返回 → "代码不正确"。防御：
   - 时间戳新鲜度（`after_ts` + `_FRESHNESS_TOLERANCE_SECONDS`）+ settle 机制；`fetched_at` 只是查询时间不算数，必须用邮件真实时间。
   - **持久化"已用验证码"**：`data_dir/otp_consumed.json`（按邮箱 casefold 归档、去重、6h TTL、每邮箱最多 20 条）。`_wait_for_email_otp` 成功后 `_record_consumed_otp` 落盘；下轮（即便另一个进程）取码前 `_consumed_otps_for` 读出来作 `exclude_codes` 传给 `fetch_latest_otp`，贯穿结构化/网页收件箱/纯文本三条抽码路径。取码器只对标注 `supports_exclude_codes` 的 provider 传排除项。
5. **新取码邮箱可能是 JS SPA 壳**（如 weimail `sms.linlinflow.ccwu.cc` 的 `/latest`）。真正 JSON 在 `/mail-api/{auth_code}/{email}?folder=inbox`；`_weimail_api_url` 负责把 `/latest` 换算成后端接口，抽不到再回退原地址。
   - **卡片式收件箱**（如 `icloud-api.top/show/...`）：HTML 直接展示邮件，结构是 `<div class="card">` 内含 `.fr/.su/.dt/.bd`。不专门解析就会掉到裸正则 `_extract_code` 兜底——**没有新鲜度校验，最坏还会 `return codes[-1]` 瞎猜**，于是取到旧码/占位码（曾出现 `000000`）。用 `_CardInboxParser` + `_extract_from_card_inbox` 做 OTP 上下文 + `.dt` 新鲜度 + `exclude_codes` 校验，且 `recognized=True` 后**绝不回退裸正则**。新增取码邮箱格式时，优先写专用解析器而不是靠兜底。
6. **MV3 service worker 会休眠**导致漏收桥接请求 → 整轮超时且无响应（区别于 `waitForTabComplete` 自己的 45s 超时）。`_bridge_navigate` 对 navigate 超时做一次性重试。
7. **OpenAI 整页错误屏别塞进"未识别页面"**。提交邮箱 OTP 后 OpenAI 可能跳到整页错误屏（错误写在 `<h1>`+副标题里，`visibleError()`（只扫 `[role=alert]/.error`）看不到），常见两类：`身份验证错误 / account_deactivated`（账号已被删除停用——**死号，别重试别浪费短信**）、`糟糕，出错了！/ Operation timed out`（OpenAI 服务抖动——**可重试**）。若不识别就统一报成 `未识别页面阶段: unknown`，被 `_failure_info` 当 `task_failed` 不重试，且日志看不出真因。
   - `background.js` 的 `pageLevelError()` 专门识别这些整页错误，回传带 ASCII 令牌的消息（`account_deactivated` / `openai_transient`）；已接入 email-OTP 阶段判定。
   - `codex_service._failure_info` 按令牌分类：`account_deactivated → 不可重试`（且 `_handle_result_locked` 落成终态 `deactivated`、邮箱标记死号）；`openai_transient` / `浏览器桥接超时` / `超时` → `transient_network` 可重试。
8. **某一步"特别慢"先查 `background.js` 对应 `waitFor` 的判据，不是网络慢**。判据没覆盖到本流程真实出现的页面时，`waitFor` 会一路空转到 30s 超时才返回 falsy（不抛错，所以只表现为卡顿）。实例：`submit_email` 的判据只认 code inputs / `/email-verification` / create-account-password / 第三方域 / "验证码"文案，**漏了老账号登录流的密码输入页** → 每次填密码前白等 30s（填 2FA 那步判据能立刻命中，所以对比明显）。已补 `input[type=password]` → `'password'` 阶段，且必须排在 `isCreateAccountPasswordPage()` **之后**（注册流的设置密码页同样有 password input）。
9. **改完 Python 一定要重启后端进程再复现**。worker 是同进程内的线程，模块在服务启动时就加载完了，改文件不生效。判据：traceback 里的**行号与当前源码内容对不上**（例如报错行显示的是注释）就是在跑旧字节码；对一下后端进程 StartTime 和文件 mtime 即可确认，别去追不存在的代码 bug。

10. **进邮箱表单要靠"点登录按钮"，不是只靠重定向；判据必须认 `<button>` 不能只认 `<a href>`**。走代理时 `chatgpt.com/auth/login_with` 常常**不重定向**，而是直接渲染登出态的聊天落地页（`今天有什么计划？`）。那个页面上有两个可点的「登录」控件，但它们是 **`<button>`（`type=""`，无 href）**——旧的 `loginLink()` 只匹配 `a[href*="/auth/login_with"], a[href*="/log-in"]`，于是判定"这页没救"，而人工点一下就能正常登录（用户实测：直连可以，走代理手点也可以，只有程序不行）。**这是判据 bug，不是 IP 被封、不是超时。**现在 `loginLink()` 同时收 `a` / `button` / `[role=button]` / `[data-testid*=login]`，先按 href 匹配登录端点、再按整段标签文本等于「登录/Log in/Sign in/继续」匹配，并排除「注册 / 免费注册 / Sign up」和社交 provider；点击最多试 3 个不同控件。**排查这类"页面能开、流程走不动"时，先去日志的 `[BridgeDOM] buttons=` 看页面上到底有什么控件，再看选择器能不能选中它**——本次就是 `buttons` 里明晃晃两个 `{"text":"登录"}` 而选择器只找 `<a>`。另外 `_login_account_in_browser` 保留了入口阶梯 `_LOGIN_ENTRY_URLS`（`chatgpt.com/auth/login_with` → `auth.openai.com/log-in` → `chatgpt.com/`）作为兜底，每个入口最多"提交一次 + 普通重载一次"；`chrome.tabs.reload` 别加 `bypassCache:true`（硬刷会重下 JS chunk，走慢代理适得其反）。
11. **`Cannot access contents of url "chrome-extension://..."` = 活动页解析拿到了不能注入的页面**。`getActiveTab()` 原来是"查到什么用什么"，一旦当前活动标签页是扩展自己的页面 / `chrome://` / 应用商店 / `file://`，`executeScript` 就抛这句英文报错。现在 `tabIsDrivable()` 过滤：活动页不可用就在同一窗口里另选一个并激活，都不行就新开一个 `about:blank` 标签页。**注意 `about:blank` 和空 URL 必须算"可用"**——`cleanupBrowserState` 每轮都会把标签页停到 `about:blank`，把它判成不可用会导致每个账号都新开标签页。显式绑定的标签页（gcash）不可注入时直接报中文原因，不再等到注入时才抛 Chrome 的英文错误。
12. **`提交邮箱后进入阶段：unknown` 先看标签页 URL，别急着判死**。页内判据活在**一个 document 里**、且只认写死的那几种语言；慢代理下页面跳到 `/email-verification` 可能**晚于**判据窗口关闭——此时验证码其实已经发出，直接失败等于白扔一次。所以 `_wait_for_post_email_landing()` 先用 `tab_url` 桥接**轮询标签页自身 URL** 最多 25s：看到 `/email-verification` → 当 `otp` 继续；`/create-account/password`、`/phone-verification` 同理。**这个判断不注入、不受页面语言影响**，是对付本地化界面最稳的一招。只有 URL 一直不动才落到失败分支：先把 `state`（url/title/headings/buttons/body_preview）打进日志再报错，而不是像最早那样傻等 90s 取码然后报 `HTTP 200 但未提取到验证码` 甩锅给取码邮箱。
13. **「你的会话已结束」中转页会在两个位置出现，两处都必须"看到登录控件就再点"**。`auth.openai.com/log-in` 的 `is_missing_session` 过渡页（`<a data-login-web-auth-control href=".../auth/login_with">登录</a>`）**既可能挡在邮箱表单之前，也可能在提交邮箱之后又弹回来**。一次点击不一定够——每次点击都是一次新请求、会多带一层会话上下文。扩展两处都判 `sessionEndedPage()`（无邮箱框 + 无验证码框 + 有登录控件 + 文案含"会话已结束"或 HTML 含 `is_missing_session`）：表单前那处抛 `login_retry_click`；提交后那处先给 6s 宽限（可能只是下一页还没画出来），确认还是过渡页再抛同一个令牌。Python 见到该令牌（以及点击导致的帧销毁）就**在同一个入口重跑 `submit_email`**（该步以点击开头），最多 `_LOGIN_CLICK_ROUNDS`(6) 轮才换入口。**教训：判据只写在"进表单之前"是不够的，同一张页面在流程后段再次出现时会被当成"未识别页面"直接失败。**
11. **代理跑起来后，"活动页"模式极易被误导**。仅登录/Session 导出走的是**当前活动标签页**；如果你在同一个窗口手动打开代理商的 IP 检测页（如 `ip.oxylabs.io/location`）去确认代理生效，流水线就会把那个页面当登录页操作，报出 `未找到邮箱输入框 @ https://ip.oxylabs.io/location` 这种一眼就看得出跑错页的错误。要查代理请另开一个窗口，或用「调试」页的标签页绑定。
14. **`Continue with <X>` 永远是"切换登录方式"，绝不是邮箱表单的提交按钮**。登录页 DOM 顺序是 `Continue with Google / Apple / Microsoft / **Continue with email** / 国家选择 / Continue`。旧的 `SOCIAL_RE` 只排除了中文 `电话|手机号`（漏了英文 `phone`），前三个社交按钮被排掉后，`candidates.find(CONTINUE_RE)` 选中的第一个就是 **「Continue with email」**——填完邮箱去点它，等于把表单**切到手机号模式**，URL 变成 `/log-in?usernameKind=phone_number`，页面上只剩 `input#tel`。**表现极具迷惑性：`submit_email` 不抛错、正常返回，只是 `next_stage` 为空**，日志写「提交邮箱后进入阶段：unknown」，最后被判成"未识别页面"——看着像页面认不出，其实是我们自己点错了按钮把模式切走了。修复：① `notSocial` 用 `CONTINUE_WITH_RE = /^(continue|log ?in|sign ?in|继续|登录)\s*(with|使用|用)\b/i` 一律排除任何 "Continue with …"；② `SOCIAL_RE` / `PROVIDER_LABEL_RE` 补上英文 `phone|tel\b`；③ 新增 `phoneOnlyLoginForm()`（无邮箱框 + 无验证码框 + 有 tel 框）作为提交后判据，命中就抛 `login_retry_click` 令牌，让 Python 重跑 `submit_email`——它的点击循环开头正好会点「Continue with email」切回来。**判据改动请用快照里的真实标签做一次新旧对比再提交**，别只看代码想当然。
15. **"清理了"不等于"清干净了"，要用回执验证**。`cleanupBrowserState` 曾有两个逃逸口，实测表现为 OpenAI 弹 `/choose-an-account`（"Welcome back"，上一个账号赫然在列）、日志里 `[BridgeDOM] body_text=... How can I help, <上个账号>?`：① `chrome.cookies.getAll({domain})` **不带 `storeId` 只查默认 cookie store**，隐私窗口是独立 store，而 `chrome.browsingData` 根本不碰隐私模式数据；② 只把**活动标签页**停到 `about:blank`，其它还开在 chatgpt.com 上的标签页，其页面 JS / service worker 会在擦除后立刻拿内存里的 token **把会话 cookie 写回来**——正好造成"auth.openai.com 清了、chatgpt.com 没清"的不对称（auth 域没有活标签页）。修复：`allCookieStoreIds()` 遍历所有 store、`parkTabsOnCleanupOrigins()` 先停掉所有停在目标域的标签页，**擦完再查一遍并回传 `remaining_cookies`**，Python 侧照实打日志（干净/残留各一条）。以后排查这类问题**先看这条日志，不要凭"代码里有清理"就认定清干净了**。
17. **`isCreateAccountPasswordPage()` 的兜底判据曾把"登录密码页"当成"注册设密码页"**，后果是把**自己生成的注册密码填进了现有账号的登录框**，页面报 `Incorrect email address or password`（快照 `logs/debug/1038377890-20260811-152320-screenshot.log`：url=`/log-in/password`、`input[name=current-password]` 里赫然是我们生成的 12 位密码）。原兜底是 `passwordInput() && /create account|password|创建|设置/`——**任何密码页的文本里都有 "Password"**，必然命中。于是 `submit_email` 回报 `create-account-password`，注册流程跳过 `continue_with_password` 直接去 `submit_signup_password`。**硬区分点：登录页是 `current-password`，注册设密码页是 `new-password`**（`isLoginPasswordPage()`）。兜底文案也收紧成 `create an? account|create a password|set a password|设置密码`。
18. **地址已被注册 = 终局，必须单独分类**。这类号既注册不了（已存在）、也登录不进去（密码不是我们的），重试只会再烧一个取号。`_ACCOUNT_EXISTS_TOKEN = account_already_registered` 由三处抛出：① `submit_email` 后阶段为 `login-password`；② 点「Continue with password」后落到 `login-password`；③ 兜底——提交密码被回 `Incorrect email address or password`。`_failure_info` 里这条**必须排在 `register_failed` 和"超时可重试"之前**，落成不可重试的 `account_already_registered`，消息含「已注册帐号」。
19. **改页面判据必须拿真实快照的标签/属性做新旧对比再提交**。本轮两个判据 bug（"Continue with email" 被当成邮箱提交按钮、登录密码页被当成注册设密码页）都是"看代码觉得没问题、跑起来必错"，而用快照里的真实 label / `name` 属性跑一遍对比表，一眼就能看出旧判据选错了对象。
20. **注册入口和登录入口是两回事，混用必死**。`/log-in` 是**登录**页——往里填一个全新地址，OpenAI 回的是「Enter your password」(`/log-in/password`)，`/email-verification` 根本不会出现。注册必须走 **`https://auth.openai.com/create-account`**(`_SIGNUP_ENTRY_URLS`)，才会给出验证码页，再由「Continue with password」进 `/create-account/password` 设密码。历史 bug：`_run_account_signup` 图省事复用了 `_open_login_and_submit_email` 的默认 `_LOGIN_ENTRY_URLS`，每个新号都被带去登录密码页，我们还把自己生成的密码提交上去、拿到 `Incorrect email address or password`。**更坏的是这个现象极像"该地址已被注册"**——当时就是这么误判的，差点让"已注册帐号"分类把大批好号白白丢掉。判"已注册"之前，**先确认入口用的是 `/create-account`**。已用 `tests/test_smsbower_gmail.py::test_signup_uses_the_create_account_entry_not_the_login_page` 锁住。
21. **"你的会话已结束"中转页的「Log in」链接是一扇单向门：跟过去就从注册掉进登录**。

## 编辑注意

- 用 `Edit` 插入新函数时，`old_string` 要用**已存在的锚点**（如 `def _decode_data_uri`），别把要新增的内容当 old_string。
- 给大段代码重新缩进后用 `ast.parse` 验证 Python；JS 用 `node --check chrome_plus_ver/background.js`。
22. **可信点击的坐标绝不能"提前算好"——元素会在派发前挪走**。`trustedClick` 的链路是：注入侧算坐标 → 塞进 `window[__cdpc_*]` 队列 → service worker 轮询（90ms）+ 一次注入往返 → 才真正 `Input.dispatchMouseEvent`，**中间隔 100~300ms**。而 `scrollIntoView({block:'center'})` 会滚动最近的可滚动容器（MFA 弹窗那个 `overflow-y-auto` 内容区就是），原来只 `sleep(80)` 就读 rect，等 SW 派发时按钮早滚到别处，**点击落在空白上、CDP 照样报成功**——现场表现是「点击「Trouble scanning?」后没有显示 Base32 密钥」，看着像页面不给密钥，其实压根没点中。修复三处：① 滚动改 `behavior:'instant'` 并**轮询等 rect 稳定**（连续两次位移 <0.5px）再取坐标；② 给目标打一次性 `data-cdpc-target` 标记，**SW 在派发前重新量一次坐标**，并用 `document.elementFromPoint` 确认那个点上最顶层元素就是目标本身，不是就回 `covered`/`gone` 让注入侧明确报错；③ 调用方**必须检查 `trustedClick` 的返回值**——以前 `mfa_reveal_secret` 不看返回值，点空了照走下去，把"没点中"伪装成了"页面没给密钥"。**加任何新点击时，返回值必须检查。**清完 cookie 后导航到 `/create-account`，OpenAI 先给的是 `is_missing_session` 中转页（`headings=["Your session has ended"]`），**这页唯一的控件就是「Log in」**。原来的 `sessionEndedPage()` 恢复逻辑照单点下去 → `chatgpt.com/auth/login_with` → 整条流程被换成**登录**流程；后面拿到的 `/email-verification` 是登录版的，它的「Continue with password」去 `/log-in/password` 而不是 `/create-account/password`。**表象极具误导性**：日志显示"提交邮箱后进入阶段：otp"（步骤 2 看着是过的），失败却出现在步骤 3，还被误读成"这个地址已被注册"。修复：`_open_login_and_submit_email(signup=True)` 时遇到该中转页**不点任何链接**，而是**重新请求同一个 `/create-account`**——中转页本身已经把 session 种好了，第二次请求就是真表单。判"已注册"之前先确认全程没跟随过 `登录` 链接。

23. **「未找到邮箱输入框」≠「这个入口是死路」——先看是不是**已经走过邮箱这一步了****。现象：页面明明已经到了 `auth.openai.com/email-verification`（`Check your inbox`、code 输入框、底部「Continue with password」都在），流程却卡住不动，日志刷「入口 … 未落到邮箱表单，重新加载后再试一次」（`logs/codex-d230681a1023b30343738ede-ed6497f6-a1.log`）。成因：`submit_email` 会被**重跑**（`login_retry_click` / 帧销毁都会重跑），而点回「你的会话已结束」的登录控件后，OpenAI 直接把我们送回**已经发起的那次验证**，那页当然没有邮箱框 → 注入侧抛「未找到邮箱输入框」→ Python 按 `_LOGIN_ENTRY_DEAD_END` 当死路重新加载入口，**把已经拿到的进度整个扔掉**（还白烧一个取号）。修复两层：① 注入侧 `submit_email` 在找邮箱框**之前**、以及 `inputNode` 为空时，都跑一次 `alreadyPastEmail()`（`/email-verification`→otp、`/phone-verification`/`/add-phone`→phone、create-account-password、login-password、有 code 框→otp），命中就直接 `ok:true` 带 `already_submitted` 返回，让上层照常去点「Continue with password」；② Python 侧只对 `_NO_EMAIL_BOX_TOKEN` 这一个令牌加兜底：判死路前用 `tab_url` 探 3s（不注入、不受页面语言影响），已经在后续页就合成 `next_stage` 继续，**绝不重进入口**。`login_with_shell` 不走这条兜底（注入侧的新判据排在它之前，两者不可能同时成立）。有测试锁这条。
24. **一个步骤只要会被重跑，就必须能认出"这一步已经做过了"**。第 23 条不是孤例：`submit_email`、`submit_email_otp`、finalize 都有重跑路径。判据只写"这一步该有的控件在不在"是不够的——控件不在，可能是**页面已经往前走了**。改任何会被重跑的步骤前，先问一句"如果它已经成功过一次，这段代码会怎么判"。

25. **点击导致的导航会销毁注入帧，凡是"点了就会跳页"的步骤都必须容忍 `Frame with ID 0 was removed`**。现象：日志停在 `切换到密码注册`，随后 traceback `RuntimeError: Frame with ID 0 was removed.`，而浏览器里页面**明明已经好好地停在 `https://auth.openai.com/create-account/password`**（`logs/codex-d230681a1023b30343738ede-35828900-a1.log`）。成因：`continue_with_password` 当时是裸的 `_bridge_page_action`，没有像 `_submit_login_step` 那样容忍帧销毁——点击成功导致的导航反而被当成致命错误，把一个**验证码已经发出**的账号直接作废，还白烧一个取号。修复：新增 `_switch_to_password_signup()`，帧销毁/桥接超时一律改判"点成功了"，再用 `_wait_for_password_page()` 读标签页 URL 定落点；页内判据返回空时也用它兜底。**这个探测刻意不复用扩展的 `inspectAuthFlowStage`**——它把 `/log-in/password` 也算成 `create-account-password`，正是踩坑 #17 那个"把自己生成的密码填进登录框"的误判；`_PASSWORD_PAGE_URLS` 明确区分两者，且只认密码页（还停在 `/email-verification` 就继续等，不会把"还没跳"当落点）。扩展侧 `executePageStep` 的帧销毁恢复名单（`submit_email_otp` / `submit_signup_password`）**没有**加 `continue_with_password`，就是为了避开那个误判。有测试锁这两条。

26. **帧销毁的第二种表现是"静默返回 null → 页面动作返回空结果"，别只处理抛异常那一种**。踩坑 #25 修完 `continue_with_password`，紧接着**下一步** `submit_signup_password` 又以这种形式死了一次（`logs/codex-d230681a1023b30343738ede-1e3b489f-a1.log`：日志停在「设置账号密码」，11s 后 `RuntimeError: 页面动作返回空结果`，而密码其实已经设好、页面已经在 `/email-verification` 上，连 120s 的取码窗口都没进）。`_FRAME_TEARDOWN_ERROR` 早就同时认这两种写法，问题一直是**调用点是裸的 `_bridge_page_action`**。现已统一：注册路径里所有"点了就会跳页"的步骤都过一层包装 —— `_switch_to_password_signup`（Continue with password）、`_submit_signup_password`（设密码）、`_click_signup_link`（Sign up 恢复），帧销毁/桥接超时一律改判"做成功了"，再用 `_wait_for_tab_stage(targets=…)` 读标签页 URL 定落点。**`targets` 只列"已经往前走了"的页面**，页面还没跳就继续等，绝不把原地当落点；有前缀关系的要素必须按长到短排（`/create-account/password` 排在 `/create-account` 前，否则设密码页会被认成邮箱表单再白提交一次邮箱）。加新的点击步骤时照这个模式来，别再裸调 `_bridge_page_action`。有测试锁这几条。
。踩坑 #25 修完 `continue_with_password`，紧接着**下一步** `submit_signup_password` 又以这种形式死了一次（`logs/codex-d230681a1023b30343738ede-1e3b489f-a1.log`：日志停在「设置账号密码」，11s 后 `RuntimeError: 页面动作返回空结果`，而密码其实已经设好、页面已经在 `/email-verification` 上，连 120s 的取码窗口都没进）。`_FRAME_TEARDOWN_ERROR` 早就同时认这两种写法，问题一直是**调用点是裸的 `_bridge_page_action`**。现已统一：注册路径里所有"点了就会跳页"的步骤都过一层包装 —— `_switch_to_password_signup`（Continue with password）、`_submit_signup_password`（设密码）、`_click_signup_link`（Sign up 恢复），帧销毁/桥接超时一律改判"做成功了"，再用 `_wait_for_tab_stage(targets=…)` 读标签页 URL 定落点。**`targets` 只列"已经往前走了"的页面**，页面还没跳就继续等，绝不把原地当落点；有前缀关系的要素必须按长到短排（`/create-account/password` 排在 `/create-account` 前，否则设密码页会被认成邮箱表单再白提交一次邮箱）。加新的点击步骤时照这个模式来，别再裸调 `_bridge_page_action`。有测试锁这几条。

27. **重跑的账号：密码要沿用上一轮那个，落到 `/log-in/password` 更不能判死刑**。现象：`register_failed：切换密码注册后进入了 unknown 页（需要 /create-account/password）`——实际是「Continue with password」落到 `/log-in/password`（这个地址上一轮已经被我们建号并设过密码了，OpenAI 认得它），然后 `Sign up` 恢复报「登录密码页上没有「Sign up」链接」，回 `""` → unknown → 判死（`logs/codex-d230681a1023b30343738ede-1b3e6762-a1.log`）。正解是**把上一轮存下来的那个密码填回去**，填对之后 OpenAI 要求邮箱验证，下一步正好是 `/email-verification`，接回取码流程。实现：① `_run_account_signup` 开头 `store.get_secret(email=...)` 取 `known_password`，有就**沿用**，不再另生成——另生成一个新密码等于再也登不进那个已存在的账号（`update_registration` 一直是在提交密码**之前**落盘的，正是为这一刻）；② 两个 `login-password` 落点（提交邮箱后、Continue with password 后）在 `known_password` 非空时都走 `_resume_with_known_password()`（`submit_password` + `_wait_for_tab_stage` 读 URL 判落点），成功就置 `resumed_with_password`，**跳过整个"设置密码"步骤**直接等验证码；③ 密码被判 `Incorrect email address or password` 才是真终局，分类成 `account_already_registered`。**注意这条和踩坑 #17/#20「注册阶段绝不能走登录密码页」不矛盾**：那两条针对的是**手上没有密码**的全新地址；这里账号是我们自己建的、密码在库里，所以只在 `known_password` 非空时才走这条路。有测试锁这几条。

28. **2FA 密钥这一步绝不允许"试一次就放弃"，更不允许跳过账号**。密钥 OpenAI **只显示这一次**，读不到 = 这个账号废掉，所以宁可卡住等人也不能往下走（`logs/codex-f6180c744a1706da3987e212-95379c0c-a1.log`：弹窗明明开着、`#totp_otp` 都在，一句「点击「Trouble scanning?」后没有显示 Base32 密钥」就把账号记成"未开 2FA"过去了）。真正的 bug 是那段重试循环 `if (await trustedClick(button)) { await waitFor(...); **break**; }` —— 点中了就 break，等于只试了一次。现在三级兜底，由 `_reveal_totp_secret()` 串起来：① **重试**：Python 侧 3 轮 × 扩展侧每轮最多点 3 次「Trouble scanning?」，每次点完都重新读；② **解二维码**：`mfa_capture_qr` 用 CDP `Page.captureScreenshot` + clip 把弹窗里的二维码截下来（对 `<canvas>`/`<svg>`/跨域 `<img>` 一视同仁，不走"画进 canvas"那条会被跨域污染的路；clip 用**页面坐标**要加 scrollX/Y，scale 3 + 12px 白边给解码器留 quiet zone），Python 用 opencv 解 `otpauth://` 取 secret，**正反相各试一遍**（深色模式二维码是反的）；③ **转人工**：`_wait_for_manual_totp_secret` 每 5s 用 `read_only` 模式**只读不点**（免得和人抢焦点）轮询最多 30 分钟，人在浏览器里把密钥点出来就自动接上继续跑，每分钟额外再试一次二维码并推一条 notice。**唯一的出口是用户自己点「停止后续任务」**（`cancel_event`），或等满 30 分钟。另外密钥判据放宽了：不再只认 `[aria-label="Copy code"]`/`code`/`.font-mono`，改成先认这些控件、再扫弹窗里的叶子节点，并且**必须含 2-7 的数字**才算 Base32（Base32 字母表就是 A-Z2-7，只判字母的话 `AUTHENTICATORAPP` 也会命中）；DOM 或 React props 里能直接读到 `otpauth://` 时连截图都省了。`opencv-python-headless` 已加进 requirements.txt，**没装也能跑**（自动降级成人工等待并在日志里说清楚该装什么）。有测试锁这几条，含用真二维码走完整解码。

29. **"可信点击落空 / 被遮挡"是瞬时状态，绝不能一次就判死**（2026-08-12）。同一个账号连跑两遍：第一遍在「设置账号密码」报 `可信点击落空：目标点被其它元素遮挡（可能有未关闭的弹层）@ /create-account/password` 整单作废，隔 3 分钟原样重跑一次一路通到开完 2FA（`logs/codex-2ffe475cee4fb8bb73aa7c70-1d27c48f` vs `-cbd917a5`）。差别只在**时机**：失败那遍是点「Continue with password」触发帧销毁 → `_wait_for_tab_stage` 一看到 URL 变成 `/create-account/password` 就**立刻**注入下一步，页面还在做视图过渡（旧视图淡出层压在新视图上）；成功那遍中间多隔了 1 秒。三层修复：① 扩展 SW 的点击泵重新量坐标时**连量 12 次 × 150ms**，真的一直被挡着才回 `covered`，并把遮挡者的标签/类名打进 console（以前日志里只有"被遮挡"，下次还得从头猜）；② 注入侧 `trustedClick` 外面包一层完整重试（重新滚动 + 重新排队 ×3），因为元素可能是**永久挪位**、只重量坐标没用；③ Python 侧 `_wait_for_tab_stage` 匹配到落点后统一静置 `_PAGE_TRANSITION_SETTLE_SECONDS`(1.2s) 再返回，且 `_submit_login_step` / `_submit_signup_password` / `_switch_to_password_signup` 遇到 `可信点击落空` 一律整步重跑（`_page_action_with_click_retry`，最多 3 次，其它错误立刻抛）。**重跑是安全的**：SW 在派发前会确认目标就在那个点上，落空意味着那一下点击**根本没派发出去**，不存在重复提交。有测试锁这两条。

30. **落盘失败绝不能杀死调度线程；状态文件必须有上界**（2026-08-13）。现象：整条流水线"死了"——不取号、不派发下一个、界面上还是"运行中"，而且**没有任何报错**（`logs/codex-89a3908a8c6a31c42b8435ca-8c25fab1-a1.log` 只记录了这一单自己的失败）。真因在 `codex_service`：`_persist_locked` 的 `os.replace(临时文件, pipeline-state.json)` 在 Windows 上撞到别的句柄（杀软实时扫描 / 同步盘）抛 `PermissionError`，而 `_run_pipeline` 的 `while True` **没有 try** → 调度线程当场死亡 → `inflight` 永不清空 → `_supply_due()` 永远 False（不取号）、`all_terminal` 永远 False（不收工）。
    - **怎么认出来**：① `data/` 下留着孤儿 `.pipeline-state.json.<uuid>.tmp`（临时文件写完了，`os.replace` 那步失败）；② `pipeline-state.json` 的 mtime 停在故障时刻，里面那个 job 还是 `queued`/`attempt=0`/`log_path=None`，但 `logs/` 下它的 `-a1.log` 明明存在；③ `data/mailboxes.json` 里同一个账号 `register_status='failed'`（worker 自己写的）而 `codex_status='running'`（调度器写的那半没跑）——**这一对矛盾直接证明 `_handle_result_locked` 从没被调用**。
    - **修复分三层**：① `_persist_locked` 对 `PermissionError` 短退避重试 6 次，最终失败也只记 warning 后返回（内存状态才是权威，丢的只是重启后的可恢复性），并删掉半成品 `.tmp`；② `_run_pipeline` 的循环体整体 `try/except`，任何一轮的意外只跳过这一轮；③ `MailboxStore._write` 是**一模一样**的裸 `os.replace`，同样能杀死调度线程（`update_codex` 就在 `_dispatch_locked`/`_handle_result_locked` 里），也加了重试——但**故意不吞异常**：那个文件里存着账号密码和 2FA 密钥，静默丢一次写入等于废掉一个账号，必须让调用方失败。
    - **`pipeline-state.json` 必须有上界**（`_RETAINED_JOBS`=200 + `_archive_old_state_locked`）。job 只增不减，实测已到 657 条 / 855 KB，而这个文件**每次派发、每个结果**都全量重写——文件越大、写得越频，撞 `os.replace` 的概率越高。超出的部分挪进 `data/pipeline-archive/{YYYY-MM}.json`（实测 855 KB → 240 KB）。**活跃流水线和活跃 job 一条都不动**（哪怕它 `created_at` 最老）；归档只发生在**启动时和新建流水线时**，绝不在热路径上；**归档没写成就一条都不删**。取 200 是因为 `list_jobs()` 本来就只回最近 500 条。`mailboxes.json` 同样是全量重写，但它是业务数据（账号要全部显示），**不归档**，只加重试。
    - **不要因为这次事故去换 SQLite**：根因是"落盘异常能杀线程"，与存储格式无关；SQLite 照样有 `SQLITE_BUSY`，那层 try 一个都不能省。真要换，也只换成"SQLite 做落盘层、内存仍是权威"（热字段建列 + 其余塞 JSON 列，那 41 处 `job.update(...)` 一行都不用改）。有测试锁上面这几条。

31. **`/about-you` 的年龄输入框有两种格式，必须兼容**（2026-08-14）。老格式是单个 `input[name="age"]` 或 `input[type="number"][inputmode="numeric"]`；新格式（快照 `logs/debug/1038378191-20260813-210608-screenshot.log`）是 **Birthday 分段输入**：三个 `div[role="spinbutton"][contenteditable="true"]`（MM / DD / YYYY）+ 一个 `input[type="hidden"][name="birthday"]`。`fillAboutYou()` 和 `submit_about_you` 都要识别 `birthdayHidden`：有就从 `age` 参数往回推 N 年算出生日（月日随机 1-28 号，安全覆盖所有月份），分别填进三个 spinbutton。**别只认老格式**——新格式下会报"未找到年龄输入框"直接失败。有测试 `test_about_you_birthday.py` 锁这两条。
